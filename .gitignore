# Machine-local panel state (pinned session ids + window position)
config.json

# Backups created by install.ps1
*.bak

# Local-only assistant guidance / personal workflow notes
CLAUDE.md

# Local-only issue tracker (contains real project/session names)
TODO.md
